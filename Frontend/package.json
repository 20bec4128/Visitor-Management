{
  "scripts": {
    "dev": "npm --prefix visitor-app run dev",
    "build": "npm --prefix visitor-app run build",
    "preview": "npm --prefix visitor-app run preview",
    "lint": "npm --prefix visitor-app run lint"
  },
  "dependencies": {
    "axios": "^1.14.0",
    "react-router-dom": "^7.13.2",
    "react-webcam": "^7.2.0"
  }
}
